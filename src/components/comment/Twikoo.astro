---
import { commentConfig } from '@/config'

interface Props {
  path: string
}

const config = {
  ...commentConfig.twikoo,
  el: '#tcomment',
  path: Astro.props.path,
}
---

<div id="tcomment"></div>
<script is:inline src="https://registry.npmmirror.com/twikoo/1.6.40/files/dist/twikoo.min.js"></script>
<script is:inline define:vars={{ config }}>
  twikoo.init(config)
</script>
